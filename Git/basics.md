Git is a distributed version control system.  
The command `git init` creates a new Git repository by initializing a `.git` directory.  
A repository is where Git tracks and stores all changes made to the project.
 